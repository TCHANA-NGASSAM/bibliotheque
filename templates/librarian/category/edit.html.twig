{% extends 'base.html.twig' %}

{% block title %}Modifier catégorie{% endblock %}

{% block body %}
    <h1>Modifier {{ category.name }}</h1>
    {{ form_start(form, { attr: { class: 'stack-form max-w-form' } }) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
    <p><a href="{{ path('librarian_category_index') }}">← Liste</a></p>
{% endblock %}
