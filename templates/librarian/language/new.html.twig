{% extends 'base.html.twig' %}

{% block title %}Nouvelle langue{% endblock %}

{% block body %}
    <h1>Nouvelle langue</h1>
    {{ form_start(form, { attr: { class: 'stack-form max-w-form' } }) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Créer</button>
    {{ form_end(form) }}
    <p><a href="{{ path('librarian_language_index') }}">← Liste</a></p>
{% endblock %}
